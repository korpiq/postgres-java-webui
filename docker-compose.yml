services:
  postgres:
    image: postgres:18-alpine
    container_name: postgres-java-webui-db
    environment:
      POSTGRES_DB: pogrejab_db
      POSTGRES_USER: pogrejab
      POSTGRES_PASSWORD: pogreJabPass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U appuser -d appdb"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
